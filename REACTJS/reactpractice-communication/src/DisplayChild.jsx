const DisplayChild = ({ handleChange }) => {
    return (
        <input
            type="text"
            placeholder="Type "
            onChange={handleChange}
        />
    );
};
export default DisplayChild;
